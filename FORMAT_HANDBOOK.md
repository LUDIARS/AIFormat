# 利用者向け説明の共通設計

## 目的と適用範囲

利用者が「自分の目的に使えるか」「次に何をするか」「成功したか」を判断できる説明を、各製品の画面や操作から開けるようにする。初期対象は Anatomia、Praeforma、GLAB、Voluptas。Cc は共通設計の基準製品とせず、必要になった時に同じ契約を採用する。

本書は説明原稿と表示側の共通契約であり、共通UIライブラリや配信サーバの実装を要求しない。Memoria #713 は 2026-09-08 の neco 指示により、この設計の作成を完了範囲とする。各製品の原稿作成と表示機能の実装は後続の別作業とする。

## 正本と責務

- AIFormat の本書が説明の共通ルールの正本。各製品がコピーして独自改変しない。
- 製品固有の原稿・画像・対応表はその製品リポジトリの `docs/manual/` に置く。既存原稿がある場合は統合し、同じ説明の編集可能な複製を作らない。
- [FORMAT_UX.md](./FORMAT_UX.md) の `spec/ux/` は問題・価値・体験の設計正本。説明はそこから利用者の目的を受け取り、実装済みの操作を案内する。設計案を利用可能な機能として説明しない。
- `docs/manual/README.md` は編集者向けの目次・確認記録とする。利用者向け本文に内部ファイル構成、PR、ポート、実装都合を混ぜない。
- 表示側は原稿を読み取り専用で利用する。DB・配信成果物・別サイトに生成した複製を編集の正本にしない。

## 原稿の構成

製品紹介 `overview.md` と、目的ごとの短い章 `<topic-id>.md` を用意する。画面一覧や全機能の列挙より、初めて成功する一連の操作と日常の主要操作を優先する。

各章は次を順に説明する。不要な手順を水増ししない。

1. **できること**: 誰が、どんな場面で、何を得られるか。
2. **始める前に**: 必要なデータ、権限、準備。制限がある場合は操作前に示す。
3. **操作する**: 実際の表示名で入口と手順を示す。入力例には架空データを使う。
4. **成功を確かめる**: 画面の変化・生成物・保存先など、本人が確認できる結果。
5. **困ったとき**: 代表的な空状態・エラーと回復方法。再実行が重複を生む場合や取り消せない操作は、その操作の直前にも説明する。
6. **次にできること**: 関連する章へのリンク。

製品紹介は対象者、用途、最初の成功までの道筋、主要章への入口を示す。開発者向け製品ではCLI/APIが実際の利用入口ならそれを説明する。存在しないGUIを想像しない。

用語は初出で説明し、内部略称を前提にしない。画像は必要な時だけ使い、代替説明を添える。図やスクリーンショットは本文の代わりにせず、個人情報・認証情報を含めない。

## 章の識別と画面との対応

製品紹介 `overview.md` を含む各章の frontmatter は次を持つ。型は `order` のみ整数、`source_refs` のみ文字列配列、それ以外は文字列。未確認の値を推測で埋めない。

| 項目 | 契約 |
|---|---|
| `handbook_version` | 共通契約の版。現在は文字列 `"1"` |
| `id` | 製品内で一意の小文字英数字・ハイフン。公開後は表示名変更でも維持 |
| `title` / `summary` | 利用者向けの章名 / 目的を一文で説明 |
| `order` | 目次順。同値の場合は `id` 順 |
| `status` | `draft` / `published` / `retired`。AI原稿の初期値は `draft` |
| `verified_revision` | 静的に照合した製品コードのコミットID |
| `source_refs` | 根拠となる製品内の相対ファイルパス。最低1件 |

節へのリンクは `#目的` の見出し自動変換に依存せず、ASCIIの明示アンカー（例 `<a id="start"></a>`）を使う。アンカーIDは章IDとは別の名前空間で、章内一意とする。公開後の削除・改名はリンク移行と同時に行う。

`docs/manual/contexts.json` は版付きの対応表を持つ:

```json
{
  "version": 1,
  "overview": "overview",
  "contexts": [
    { "key": "example.operation", "chapter": "first-result", "anchor": "start" }
  ]
}
```

`version` は対応表そのものの書式版で、現在は整数 `1`。`overview` は製品紹介の章IDで、未知の文脈の案内先になる。`key` は画面・機能の安定した意味ID。製品の画面側が現在の文脈をこのIDに対応付ける。URLや画面番号、製品横断のルート規則を共通設計へ持ち込まない。`chapter` は章ID、`anchor` は省略可能な節ID。`chapter` と `overview` は実在する章の `id` と一致させ、`anchor` はその章の明示アンカーと一致させる。原稿のみの段階では確認できた文脈だけを宣言し、画面への配線が未実装であることを編集者向けREADMEに記録する。

未知の文脈は製品紹介へ案内する。既知の対応先が欠けている場合は製品紹介へのリンクと説明の不備を表示し、表示側の診断に残す。原稿の読み込み失敗は空の説明で隠さず、再試行の入口を示す。

## 表示側が後続実装で守ること

- 画面の「ヘルプ」「使い方」など発見可能な入口から、その画面の文脈に対応する章を開く。製品全体の目次にも移動できる。
- 説明を開閉しても入力や作業位置を失わない。閉じたら元の入口へフォーカスを戻す。
- Webのモーダル表示ではキーボード操作、Escapeで閉じる操作、フォーカス管理、背景操作の抑止、小画面での閲覧を備える。ショートカットは任意で、入力中の文字を奪わない。
- Markdownは見出し・段落・一覧・表・コード・リンク・画像・明示アンカーを基本とする。任意HTMLやスクリプトは実行しない。アンカー以外のHTMLが必要なら別途仕様化する。リンクの危険なスキームや原稿ディレクトリ外へのファイル参照を拒否する。
- `draft` は通常の利用者向け表示に出さず、編集用プレビューで区別する。公開の承認者と対象コミットはREADMEに記録する。原稿生成やPR作成だけでは公開承認と扱わない。

## 更新と受け入れ

画面の表示名・操作・権限・結果が変わる変更では、対応する原稿も同じ変更単位で更新する。静的確認と実操作確認は分けて記録し、実行していない確認を済みと書かない。

原稿作成の完了条件は、紹介と主要シナリオが揃い、各章に操作根拠と確認コミットがあり、対応表とリンクが一致し、未確認事項がREADMEに残っていること。初期原稿では `draft` のままcommit・PRまで進めてよい。公開・画面配線・実操作検証の完了は含めない。

原稿担当は [章テンプレート](./examples/handbook/chapter.md) を使い、現行UX定義・既存説明・画面/CLIコードを照合する。機能の不足を補う実装や、未確認機能の断定はしない。

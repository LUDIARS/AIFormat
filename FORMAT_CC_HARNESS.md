# Ccハーネスの設定・受入・実観測

2026-09-10登録。実装正本はConcordiaの `spec/feature/harness-reliability.md` と
`spec/feature/project-harness-policy.md`。本書は横断設計の入口であり、Ccの実装や運用状態を複製しない。
登録時点は作業ブランチ上の検証済み実装で、本番反映・クライアントのフック信頼承認は未確認。

## 設定と受入条件

Cc `/projects` のプロジェクト設定を正本にする。

| 設定 | 必須にするもの | 確認する境界 |
|---|---|---|
| `ddd_enabled` | UX・ドメイン定義、コードの所属 | 編集前フックと提出前 |
| `tests_required` | 変更コードに対応するテスト実装 | 提出前 |
| `ontime_tests_required` | 通常テストに加え、observe契約・述語・計測実装 | 提出前 |
| `contract_enabled` | セッション作業契約 | 既存の編集ゲート |

テスト設定は既定false。テスト実装を必須にする設定は、テスト実行・サービス再起動・
デプロイの許可ではない。構造的な証跡検査、実行成功、設計品質、実運用の観測を混同しない。
対応表は `cc.acceptance.json` version 1、`implementations` に `source`、`tests`、`contracts` を記載する。
ソースとテストの意味的な対応・十分性はレビューで確認する。

## LLM不要のオンタイムテスト

業務処理に契約を残し、実運用の呼び出しで条件を検査する。生成AIによる採点・要約は使わない。

1. `augur.contracts.json` version 1で関数/メソッド、述語module、契約ID、条件を宣言する。
2. 純粋な `pre` / `post` / `postThrow` / `invariant` 述語を用意する。秘密や入力原文をログへ渡さない。
3. Ccの `tools/concordia-ontime.mjs install --augur <Augur/bin/augur.mjs>` がAugurで計測を挿入する。
4. 同スクリプトの `lint` で契約の所属・挿入箇所を検査する。
5. 通常処理から得たJSONLを `report --since <ISO> [--logs-dir <dir>]` で集計する。

処理全体はスクリプトで再実行でき、モデル・クラシファイアへの接続を必要としない。
`observe` / `sample: 1` を使う。違反・述語例外・ログ出力失敗で本来の結果や例外を変更しない。
ログには契約ID・計測marker・時刻・判定を残す。実行されていない契約は未観測で、成功にしない。
Ccは共通log-weaverの契約runtimeが未実装のため、Augur互換のローカルadapterを使う。
受入は現在のmarkerと期間を照合する。隔離テストの結果を本番での効果として報告しない。

## フックと判断の分担

- 圧縮前: 保存済み決定事項、未回答質問、作業契約、Augur条件、直近記録を保存し読み戻す。
  圧縮中のモデルへ要約を依頼しない。復帰時に短い参照を渡す。
- MCP認証: 失敗したツール応答から認証切れを通知する。403、通信障害、レート制限と区別する。
  同じツールの成功だけを回復の観測とする。定期probeはしない。
- MCP応答の誘導: 出所偽装・秘密送信など限定した組み合わせへ助言する。通常のCc claimを遮断しない。
- 調査: Pfに登録済みなら仕様を参照し、Anatomiaで実装を照合する。Pf未登録なら従来のrepo内検索。
- 作業管理: Actioの既存タスクとMemoriaの参照・記録を使い、重複作成を避ける。
- 失敗: 同一失敗3回で復旧手順を案内する。外部更新の結果不明時は既存IDで照合し、盲目的再送を避ける。

公式プロンプト指針の助言だけはクラシファイアを使う。人間の実入力に限り、初動と15分枠の中間を
確率抽出する。最大12標本/セッション。任意のアプローチ生成は同一依頼者・repo・teamの
限定履歴を使う。未知モデルは推測せず、公式推奨を強制規則にしない。

## 可視化と導入

設定はCc `/projects`、実観測はセッションの関連作業欄。未観測は正常を意味しない。
フック設定・手順スキルは `/v1/setup?provider=codex-cli` または `claude-code` から取得する。
既存設定へマージし、Codexでは `/hooks` の信頼確認を通す。クライアント非対応イベントは導入しない。
設定snapshotの公式整合性チェックは専用APIへの送信が必要で、全設定の自動収集はしない。
Discord通知は投稿受付と配送完了を区別する。組織をまたぐアプローチ投稿は行わない。

テストは隔離DB・模擬通知を使い、サービス起動テストはCc claim後にExcubitorから本体フォルダで実施する。
ネイティブ圧縮・Discord配送・本番観測は、それぞれ実際のイベントが得られるまで未確認とする。

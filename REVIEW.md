# AI Code Review Format

レビュー対象のプロジェクト種別（スタイル）に応じて、適用するレビューフォーマットを選択する。
**共通レビュー項目**はすべてのスタイルで適用し、**スタイル固有の項目**を追加で適用する。

---

## スタイル一覧

| スタイル | 対象 | インデックス |
|---------|------|------------|
| Web サービス | サーバ / API / Web フロントエンド | [web/REVIEW.md](web/REVIEW.md) |
| ローカルアプリ | デスクトップ / モバイル / CLI 等のローカル実行アプリ（センシティブ情報を扱う） | [local-app/REVIEW.md](local-app/REVIEW.md) |
| ゲームプログラム | クライアントゲーム（PC / コンソール / モバイル） | [game/REVIEW.md](game/REVIEW.md) |

各スタイルのインデックスは「共通レビュー項目」へのリンクと「スタイル固有のレビュー項目」で構成される。

### スタイル判定

以下を上から順に評価し、最初に該当したものを主スタイルとする。

1. ゲームクライアント（ゲームエンジン製。PC / コンソール / モバイル）を含む → **ゲームプログラム**
2. HTTP(S) 等で外部クライアントにサービスを提供するサーバ / API / Web フロントエンドを含む → **Web サービス**
3. ユーザー端末で実行されるデスクトップ / モバイル / CLI / 常駐アプリを含む → **ローカルアプリ**
   （非ゲームのモバイルアプリはここに該当する。端末内のセンシティブ情報を扱う前提が
   デスクトップと共通のため。ストア配布・OS パーミッション等は local-app の各項目で読み替える）
4. いずれにも該当しない（ライブラリ / 規約集 / スクリプト集等）→ スタイル固有項目は適用せず、**共通レビュー項目のみ**を適用する

複数スタイルの成果物が同居する場合（例: ゲーム + バックエンド API）は、主たる成果物のスタイルを採用し、
他スタイルに該当する部分にはそのスタイルの固有ドキュメントを併用する。
判定結果と根拠は、レビュー成果物の冒頭に 1 行で記載する。

---

## 共通レビュー項目 (Common)

スタイルに依らず全プロジェクトに適用する。実体は [`common/`](common/) に集約し、各スタイルから参照する。

| ドキュメント | 含まれるレビュー観点 |
|------------|-------------------|
| [設計レビュー](common/REVIEW_DESIGN.md) | 設計強度 / 設計思想の一貫性 / モジュール分割度 |
| [コード品質レビュー](common/REVIEW_CODE_QUALITY.md) | コード品質 |
| [脆弱性レビュー（共通）](common/REVIEW_VULNERABILITY.md) | コードレベル脆弱性 / CI/CD・サプライチェーン |
| [品質保証レビュー](common/REVIEW_QUALITY.md) | テスト戦略 / ライセンス遵守 / ドキュメント完備性 |
| [AI 活用レビュー](common/REVIEW_AI.md) | LLM 機能のセキュリティ / AI 生成コードの検収 |
| [不足機能評価](common/REVIEW_MISSING_FEATURES.md) | 機能改善 / 不足機能 |

---

## スタイル別レビュー項目

### Web サービス → [web/REVIEW.md](web/REVIEW.md)

共通項目 + Web 脆弱性（SQLi / XSS / SSRF）/ ゼロトラスト / セキュリティ強度 / **個人データ保護** / データスキーマ / SRE / パフォーマンス / クロスプラットフォーム / アクセシビリティ・国際化

### ローカルアプリ → [local-app/REVIEW.md](local-app/REVIEW.md)

共通項目 + **センシティブ情報取扱いポリシー** / ローカルセキュリティ / ローカルデータ管理 / 運用信頼性 / パフォーマンス / クロスプラットフォーム / アクセシビリティ・国際化

> ローカルアプリはセンシティブ情報を扱う前提のため、専用の[ポリシーレビュー](local-app/REVIEW_PRIVACY_POLICY.md)を追加している。

### ゲームプログラム → [game/REVIEW.md](game/REVIEW.md)

共通項目 + ゲームセキュリティ（クライアント信頼境界 / チート対策 / セーブ・課金保護）/ セーブデータ・アセット管理 / ゲームパフォーマンス / プラットフォーム互換 / アクセシビリティ・ローカライズ

### 共通のみ (common-only)

スタイル固有ドキュメントを持たないため、専用の総合評価表は無い。
`review/<日付>/REVIEW.md` に、common 各ドキュメントの総合評価を連結した表
（観点の順序は [`scripts/scores-keys.json`](scripts/scores-keys.json) の `common-only` に従う）を作って集約する。

> Web サービスには適用できるがゲーム（特にクライアント単体）では無視できるレベルの観点（ゼロトラスト / SRE のクラウド運用 / CORS・CSP / 水平スケーリング等）は除外している。ゲームにバックエンド / Web API がある場合は `web/` のドキュメントを併用する。

---

## レビュー実施手順

> 実行モデル (Sonnet 5 等) に実施させる場合は [`prompt/REVIEW_FULL.md`](prompt/REVIEW_FULL.md) の
> プロンプトを与える (実行規律を含む)。

1. **スタイル判定** — 上記ルールで主スタイルと併用ドキュメントを決定する。
2. **対象範囲の確定** — 指定がなければデフォルトブランチ HEAD のリポジトリ全体。
   各ドキュメント冒頭の表（リポジトリ / 対象ブランチ・PR / レビュー実施日 / 対象コミット範囲）を必ず埋める。
3. **前回レビューとの突合** — `review/` に前回の成果物があれば読み、前回指摘それぞれを
   **解消 / 継続 / 再発** のいずれかに分類して総合サマリに記載する。継続・再発の指摘は
   前回の指摘 ID を引き継ぐ。観点体系（`format_version`）が前回と異なる場合、
   対応観点が無くなった前回指摘は「**体系変更による対象外化**」として分類してよい
   （前回に ID が無い場合は今回から採番し、対応関係をサマリに書く）。
4. **テンプレートの記入** — 該当する全ドキュメントを `review/<YYYY-MM-DD>/` にコピーして記入する。
   - チェック項目は「何を確認するか」の探索指示。各項目を必ず **適合 / 指摘 / 対象外** のいずれかに分類する
     （チェックボックスは適合のみ ON にする）。
   - 表のセルは空欄にしない。指摘がなければ「なし」、適用されなければ「対象外（理由）」と書く。
5. **評価の確定** — 指摘 1 件ごとに重大度を付け、「評価の決定ルール」に従って各観点の A〜D を導出する。
6. **総合評価の出力** — スタイルの REVIEW.md（総合評価表）と `latest.json` を同ディレクトリに出力する。

### PR 差分レビュー（軽量プロファイル）

PR 単位のレビューでは、フルセットの成果物（`review/<日付>/` 一式 + `latest.json`）は作らない
（それはデイリー / リリース前レビューの領分）。代わりに次の軽量プロファイルで実施する:

- **対象**は差分とその影響範囲（呼び出し元 / 依存先 / 設定・スキーマへの波及）。
- **観点**は差分に関係するものだけを選択する。関係しない観点は記載自体を省略してよい
  （「対象外」の起票も不要）。
- **成果物**は PR レビューコメント（指摘は `file:line` に対するインラインコメント）と、
  指摘の重大度・件数をまとめた 1 コメントのサマリ。
- 指摘の根拠・重大度・推奨修正の記載ルールはフルレビューと同一（下記「評価の決定ルール」）。
- 実行プロンプトは [`prompt/REVIEW_PR.md`](prompt/REVIEW_PR.md)。

---

## 評価の決定ルール

### 指摘の重大度

指摘 1 件ごとに以下の重大度を付ける。セキュリティ以外の観点（設計 / 品質 / 運用）でも同じ基準で、
「放置した場合の影響」で判定する。

| 重大度 | 基準 | 例 |
|--------|------|-----|
| Critical | 悪用可能な脆弱性・データ消失・法令違反など、現状のまま運用すると即時に実害が出る | ハードコードされたシークレット / 認証バイパス / 電源断でセーブデータ破損 |
| High | 障害・侵害・重大な保守不能に直結し得るが、発生条件が限定的、または被害が部分的 | 入力バリデーション欠如（攻撃経路は限定的）/ コアロジックのテスト皆無 / 循環依存 |
| Medium | 品質・保守性・運用効率を損なうが、即時の障害・侵害には繋がらない | God Class / ドキュメント不足 / ログレベル不適切 |
| Low | 軽微な改善余地。スタイル・好みの範囲 | 命名の不統一 / 軽微な重複コード |

### 評価 (A〜D) の導出

各観点の評価は、その観点に属する指摘の**最悪重大度から機械的に導出する**。主観で上下させない。

- **A**: 指摘 0 件。チェック項目をすべて満たす（確認済みであること。下記の未確認キャップ参照）
- **B**: Medium / Low の指摘のみ。運用上の影響は低い
- **C**: High の指摘が 1 件以上。リリース前の対応を推奨
- **D**: Critical の指摘が 1 件以上。即時対応が必要

補足ルール:

- **未確認キャップ**: 指摘 0 件でも、その観点の主要チェック項目に「未確認」が残る場合は
  A を付けない（上限 B）。所見に未確認の範囲と理由を明記する。
  「見ていない」と「問題がない」を評価上区別するためのルール。
- **行単位の評価列は補助**: 各ドキュメントの観点表にある行単位の A~D 列
  （例: 設計強度の「障害分離」行）は確認範囲を構造化するための補助であり、
  そこから観点評価を平均・集約**しない**。行で問題を見つけたら指摘として起票し、
  観点評価はあくまで指摘の最悪重大度から導出する。行単位評価の記入は任意で、
  所見のみでもよい（重複記述を避けるため）。

総合評価表の**重大指摘数**には、その観点の Critical + High の合計件数を記載する。
不足機能評価の 2 観点（機能改善 / 不足機能）は評価軸を持たないため評価を「-」とし、
重大指摘数欄には High 優先度の提案件数を記載する（優先度内訳は各ドキュメント側の総合評価表に記載）。

### 指摘の記載ルール（根拠の必須化）

- 指摘には**安定 ID** を付ける。形式は `<ドキュメント略号>-<連番 3 桁>`
  （例: `VULN-001`, `DESIGN-002`, `QUALITY-003`。略号はドキュメントファイル名から一意に取る）。
  AUTOFIX の PR・次回レビューの解消 / 継続 / 再発判定は、この ID で参照する。
  継続・再発した指摘は前回の ID を引き継ぎ、新規採番しない。
- C / D に相当する指摘には、必ず該当箇所（`file:line` またはファイル名）と確認した事実を記載する。
  **コード・設定・ドキュメントで確認できないことを推測で指摘しない。**
- 「〜かもしれない」「おそらく」等の憶測では指摘を立てない。確認手段がなかった項目は所見に
  「未確認（理由）」と書き、指摘 0 件として扱う（ただし A 判定の上限は「評価 (A〜D) の導出」の
  未確認キャップに従う）。
- ドキュメント・設定・テストの**不存在**は確認可能な事実であり、指摘にできる（例: SLO 未定義、LICENSE 無し）。
- 推奨修正は「何をどう変えるか」を具体的に書く（例: 「パスワードハッシュを bcrypt に変更する」。
  「セキュリティを強化する」のような抽象的な記述は不可）。

### 対象外 (N/A) の扱い

- プロジェクトの特性上適用されない観点・チェック項目は、減点せず「**対象外**」とし、理由を 1 行で記載する
  （例: サービス間通信が存在しないため mTLS は対象外）。
- 対象外の観点は評価（A〜D）を付けず、重み付けスコアの母数からも除外する。
- 「実装されていない」と「対象外」を混同しない。必要なのに無いものは指摘、そもそも不要なものが対象外。

### 重み付けスコア（総合）

A=4 / B=3 / C=2 / D=1 として、評価を付けた観点（対象外と評価軸のない観点を除く）の平均を四捨五入し、
A〜D に戻す。内訳（A〜D の件数）を併記する。

例: A 2 / B 6 / C 4 / D 2（14 観点）→ 平均 2.57 → **B**

---

## 成果物の配置と latest.json

レビュー成果物は `review/<YYYY-MM-DD>/` に配置し、機械処理用サマリ `latest.json` を併置する。

```json
{
  "date": "YYYY-MM-DD",
  "repo": "Org/Repo",
  "style": "web | local-app | game | common-only",
  "format_version": 2,
  "weighted_score": "A〜D",
  "scores": { "<観点の snake_case キー>": "A〜D（対象外は null）" },
  "critical_count": 0,
  "high_count": 0,
  "autofix_count": 0,
  "autofix_categories": { "lint": 0, "typo": 0, "unused_import": 0, "dead_code": 0, "gitignore": 0, "toc": 0 },
  "fix_pr": null
}
```

- `scores` のキーの**正本は [`scripts/scores-keys.json`](scripts/scores-keys.json)**。
  適用したスタイルのキー列をそのまま（順序含め）使う。評価軸のない観点（機能改善 / 不足機能）は含めない。
  観点を増減したら、スタイル総合評価表と `scores-keys.json` を同時に更新し `format_version` を上げる。
- `format_version` は観点体系のバージョン（現行 2）。Anatomia 等がトレンド比較する際に
  「どの観点体系で採点されたか」を識別するためのフィールド。
- `critical_count` / `high_count` は全観点の指摘を重大度別に合計した値。
- スキーマ（必須フィールド / 値域 / date とディレクトリ名の一致 / `format_version` >= 2 での
  scores キー突合）は [`scripts/check-latest-json.mjs`](scripts/check-latest-json.mjs) が CI で決定的に検査する。
  2026-07-09 より前の成果物は legacy としてキー突合をスキップする。

---

## 自動修正ポリシー (AUTOFIX)

デイリーレビュー (`daily-code-review`) がレビュー指摘を自動修正する際の範囲と手順。
レビュー (現状評価) と AUTOFIX (実際に修正したコードの監査ログ) は別軸で扱う。

### 自動修正の対象

1. **機械的修正** — lint warning / typo / 未使用 import / dead code / `.gitignore` 漏れ / TOC ずれ。
2. **Critical / High 指摘の bounded fix** — 修正範囲が一意に確定でき、安全に PR 化できるもの。
   例: セキュリティヘッダ (CSP / HSTS / X-Content-Type-Options 等) の追加、設定のハードニング、
   入力バリデーション追加、`NOTICE` / ライセンス表示の整備、設計意図のドキュメント明記。
3. **spec 欠落の自動生成** — 実装と `spec/` の突合で検知した欠落分の生成
   ([`prompt/SPEC_GAP.md`](prompt/SPEC_GAP.md))。コードから確認できた事実のみ記載し、
   既存 spec の上書き・削除はしない (陳腐化の解消は人間に委ねる)。

### 自動修正の対象外 (REVIEW にのみ記載し、人間に委ねる)

DB スキーマ変更 / 認証・認可ロジックの再設計 / 公開 API シグネチャ変更 / 依存メジャーアップ /
大型 refactor / 新機能実装 (テスト基盤・監査ログ機構・rate limiting 等) / パフォーマンスチューニング。
= 設計判断または大型実装を要するものは Critical / High であっても自動修正しない。
AUTOFIX.md 下部の「手作業に回した指摘」に列挙する。

### PR とマージ

- 修正は必ず feat / fix ブランチ + PR 経由。`main` への直接 push は禁止。
- 修正ごとに 1 PR (同種修正をまとめない)。
- **自動マージしてよい** — CI がある repo は green を待ち、**無 CI でもマージ可**、
  **red の時だけ**止める。マージ後はブランチ削除・後片付けまで行う。
- Critical / High 由来の PR は、本文に該当 `REVIEW_*.md` の節と重大度を引用する
  (トレーサビリティのため。マージのブロックではない)。

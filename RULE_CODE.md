# コード規約 (Code Conventions)

LUDIARS 全リポジトリ共通の **コードを書くときの規約**。

本書は 2 部構成:

- **第 I 部 — プロジェクトごとに宣言する事**: コンテンツ (サービス / アプリ /
  ゲーム / ライブラリ) ごとに「何を・なぜ・どう作るか」を spec に明文化する。
  内容はコンテンツによって変わる。
- **第 II 部 — 全プロジェクト共通の基本ルール**: スタック・コンテンツに依らず
  **どのプロジェクトも必ず守る** コードの書き方。

関連規約: インフラ・アーキ基盤 [`RULE.md`](./RULE.md) / 技術スタック選定
[`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md) / データスキーマ
[`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) / テスト
[`RULE_TEST.md`](./RULE_TEST.md) / 運用・SRE [`RULE_SRE.md`](./RULE_SRE.md)。

この文書が **正本 (single source of truth)**。各ワークスペースの
コーディング支援スキル・メモリ・harness フックはここを参照／同期する
(末尾「運用」参照)。

---

# 第 I 部 — プロジェクトごとに宣言する事

各リポジトリは、実装に入る前に以下 4 点 (I-0 〜 I-3) を `spec/` 以下 (例:
`spec/design.md`) に明文化する。**第 II 部が「全部共通の how」なら、第 I 部は
「このコンテンツ固有の what / why」** であり、設計レビュー
([`common/REVIEW_DESIGN.md`](./common/REVIEW_DESIGN.md)) はこの宣言と実装の
一致を見る。

## I-0. どのドメインに属するか

- **ドメインは設計時点で決まる。** 新規実装(コード / UI)を書くときは、
  それがどのドメインに紐づくものかを実装と並行して考え、**同じ PR で
  Anatomia のドメイン定義に登録する**。正本は `spec/domains/<name>.domain.json`。
  既存ドメインの守備範囲なら `membership` へ `pathPattern` を追加する(src と tests を対で)。
- 着手前に `anatomia where` で着地ドメインを確認し、提出前に `anatomia verify` を通す。
  未宣言を残すと、穴を開けた本人ではなく**後からそこを触った PR** が審査で止まる。
- ドメイン境界はディレクトリ構成・モジュール境界と一致させる。一致しないなら、
  宣言ではなく**設計の方を直す**。
- 設計レビューはこの宣言から入る
  ([`common/REVIEW_DESIGN.md`](./common/REVIEW_DESIGN.md) §0)。

## I-1. アーキテクチャ・技術スタックを「どの観点で」採用したか

- 採用したアーキテクチャ (レイヤー構成 / プロセス構成 / 通信方式) と技術スタックを、
  **なぜそれを選んだのか** という観点とともに書く。
- スタックの既定は [`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md)。**既定どおりなら
  「既定に従う」で良い。既定から外れる場合は理由を必ず書く** (例: リアルタイム
  描画のため C++ / 低レイテンシのため WebRTC 直結 等)。
- 「流行っているから」ではなく、後述の目的・重視点 (I-2) から逆算して選ぶ。

## I-2. コンテンツの目的は何で、目的に対して何を重視するか

- このコンテンツが **何のために存在するか (目的)** を 1〜2 文で書く。
- その目的を達成するために **何を最優先で守るか (重視点)** を明示する。
  重視点は実装上のトレードオフの拠り所になる。例:
  - ゲーム → フレーム budget / 入力遅延 / 体験の手触り
  - Web サービス → データ整合性 / 可用性 / 認証境界
  - ローカルアプリ → プライバシー / オフライン動作 / 起動の速さ
  - 共有ライブラリ → API の安定性 / 後方互換 / 利用側への非依存
- 重視点が衝突したときの優先順位もできる範囲で書く。

## I-3. 設計として何を採用するか

- I-1 / I-2 から導かれる **具体的な設計判断** を書く。
  - モジュール / レイヤーの分割方針 (コア / モジュール境界は [`RULE.md`](./RULE.md) §4)
  - 主要なデータフローと状態の持ち方 (データの種類・保存先は
    [`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md))
  - 採用パターンと、あえて採用しなかった選択肢 (理由つき)
- 設計は第 II 部の基本ルール (特に SRP §1 / レイヤー依存 §3) と矛盾しないこと。
  矛盾する設計を採るなら第 II 部側の例外として理由を残す (§21)。

---

# 第 II 部 — 全プロジェクト共通の基本ルール

スタック・コンテンツに依らず必ず守る。番号は安定参照 ID として扱う
(スキル / レビュー / メモリから `§N` で参照する)。

## 1. 単一責任 (SRP) — 必須

**クラス / モジュール / 関数は単一の責任のみを持つ。**

- 1 つのクラスが「変更される理由」を 2 つ以上持ってはならない。
- 「○○ かつ △△ をする」クラスは分割の候補。名前に `Manager` `Util` `Helper`
  `Service` を付けて雑多な処理を詰め込まない (= God Class 禁止)。
- 関数も同じ。「取得して整形して保存して通知する」関数は 4 つに割る。

判断の目安: そのクラス / 関数を 1 文で説明したとき「と」「かつ」が複数回
出たら責任過多。

## 2. ファイル分割 — 必須

**1 ファイル 1 責務。複数責務が同居したら分割する。**

- **分割の判断軸は「責任」であって「行数」ではない。** 行数に固定上限は設けない。
- 1 ファイルに無関係な複数の責務を同居させない。100 行でも 2 責務なら分ける。
- 逆に、意味的にまとまった **単一責任** であれば行数が大きくても許容する。
  **必要なら 1000 行を超えてもやむなし** (例: 自動生成・大きな定数表・1 つの
  まとまった状態機械など)。ただし「なぜ分けないか (= 単一責任である理由)」を
  説明できること。
- 行数はあくまで **見直しのきっかけ** (機械的トリップワイヤ) として使う。
  大きくなったら「これは本当に 1 責務か」を点検し、複数責務なら SRP (§1) で割る。

## 3. レイヤー依存方向 — 必須

**下層は上層に依存しない。依存は一方向に保つ。**

- ライブラリ / 基盤層 (例: Pictor) は、それを使うアプリ / 上位レイヤー
  (例: Ergo / ゲーム本体) を import しない。
- 共有ライブラリは「1 サービス専用ロジック」を持たない。複数の利用者から
  使える形に保ち、単独サービス固有の処理は利用側に置く。
- 循環依存を作らない。

## 4. 命名

- 名前は **役割を正しく表す**。`data` `tmp` `manager` `doIt` のような
  無情報名を避ける。
- 真偽値は `is` / `has` / `should` 等の述語で始める。
- 嘘をつかない (`getUser` が DB に書き込む等の副作用を隠さない)。

## 5. 制御フロー

- **早期リターンでネストを浅く保つ。** ガード節を先頭に置き、本筋を
  インデント 1 段で書く。
- 過度にネストした条件分岐 (目安 3 段以上) はリファクタ対象。

## 6. DRY / マジックナンバー / ハードコーディング禁止

- コピー&ペーストによる重複は共通化する (ただし「偶然同じ」コードを
  早すぎる抽象化で無理に統合しない — 責任が同じものだけ括る)。
- マジックナンバー / マジックストリングは名前付き定数にする。
- **環境依存値 (ファイルパス・URL・ポート番号・ホスト名など) をソースに直書きしない。**
  環境変数 (`process.env.*` 等) または設定ファイル / Excubitor 注入 env から読み込む。
  個人フォルダパス (`C:/Users/<name>/...`) や絶対パスのハードコーディングは禁止。
  秘密情報のハードコーディングは §14 で禁止。

## 7. 例外・エラー処理 / フォールバック方針

- **例外の握りつぶし (空の catch) は禁止。**
- ただし LUDIARS では「best-effort で失敗しても本筋を止めない」処理
  (hook / 監視 / 通知 / pty クローズ間際の write 等) を意図的に swallow
  する場面がある。その場合は **握りつぶす理由を必ずコメントで明示** する
  (例: `catch { /* pty may be closing; best-effort */ }`)。
- 失敗を黙って成功扱いにしない。ログ or 上位への伝播のどちらかを選ぶ。

### 7.1 設定不備による無言フォールバックは禁止 — エラーにする (必須・絶対)

**必須前提 (API キー / 実行ファイル / 接続先 / 設定値 / 認証情報 等) が欠けたとき、
機能を黙って劣化版・スタブ・mock・no-op に差し替えてはならない。即・明示的に失敗
させる (§9 fail-fast)。** 「設定し忘れ」を覆い隠す degrade は、後で「動いているのに
何も起きない / 中身が空」という最悪のサイレント故障になる。

- **許容されるフォールバックは『真の capability 劣化』のみ。** 例: レンダリングで
  Vulkan 非対応の環境 → OpenGL、ハードウェアエンコーダ不在 → ソフトウェアエンコーダ
  のように、**実行環境の実能力に応じた、設計上意図された縮退**。これは設定不備ではない。
- **設定不備 (env / キー / パス / バックエンド指定の誤り・欠落) はすべてエラーにする。**
  代替実装へ自動で逃げない。
- スタブ / mock / オフライン実装は『フォールバック先』ではなく、**明示的に選択された
  ときだけ**使う (例: `*_BACKEND=stub`、テスト用の hermetic フラグ)。既定の自動選択肢に
  しない。
- 真の capability 劣化を行う場合も、それが**意図的選択であることをコメント/ログで明示**
  し、劣化した事実を観測可能にする (黙って質を落とさない)。

判断の目安: 「この分岐は環境の能力差か、それとも設定の不備か？」を問う。**不備なら
エラー。能力差で、かつ縮退が設計済みの選択肢ならフォールバック可。**

## 8. 型・公開 API

- TypeScript は `strict` 前提。公開関数 / モジュール境界の引数・戻り値は
  型を明示する (推論任せにしない)。
- `any` の濫用を避ける。やむを得ない箇所は局所化し理由を書く。
- 不適切な暗黙の型変換に依存しない。

## 9. 入力・前提条件の検証 (fail-fast / 信頼境界) — 必須

**外部入力と必須前提は処理の入口で検証し、満たさなければ即・明示的に失敗する。**

- **信頼境界を越える入力** (HTTP リクエスト / WS メッセージ / ファイル /
  環境変数 / IPC / サブプロセス出力) は、内部ロジックに渡す前に **スキーマ検証**
  する。TypeScript は Zod を既定とする ([`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md))。
  検証済みの **型付き値だけ** を内部へ流す (`any` / 生 JSON のまま奥へ運ばない)。
- **必須前提** (実行ファイル / トークン / capability / 権限 / 接続先) も入口で
  検証する。silent に握って後続で謎の挙動 (「動いているのに何も起きない」) に
  しない。**副作用を起こす前に検証する** (確保してから前提不足に気づく順序にしない)。
  前提が欠けたときに代替実装へ**無言で逃げない** — 設定不備はエラーにする (§7.1)。
- 検証失敗は握りつぶさず、呼び出し側に分かる形で明示的に拒否する (§7)。
- 信頼できない入力を **そのまま** SQL / シェル / パス / HTML に渡さない
  (詳細はスタイル別 `REVIEW_VULNERABILITY*`)。

## 10. リソース寿命 — 確保と解放を対称に — 必須

**確保した資源は、確保した所有者が、全経路で解放する。**

- 対象: 子プロセス / socket / file handle / DB 接続 / GPU リソース (mesh・texture・
  descriptor set 等) / timer / event listener / lock / マップ上のエントリ。
- **正常系だけでなく、異常系・early-return・例外経路でも必ず解放する** (`finally`
  / dispose)。「途中で return したら socket が閉じない」を作らない。
- **置き換え (再ロード / 再登録) では「新しい資源の確保成功を確認してから古い資源を
  解放する」順序を守る。** 失敗時に旧状態を壊さず、成功の積み重ねで資源が累積
  しないようにする (= 回転方式)。
- 所有権を曖昧にしない。「誰がこれを解放するのか」を 1 文で答えられること。

判断の目安: `spawn` / `open` / `create*` / `register*` を書いたら、対になる
`kill` / `close` / `destroy*` / `unload*` がどの経路でも必ず通るかを確認する。

## 11. 非同期・並行処理

- **floating promise を作らない。** 非同期呼び出しは `await` するか、意図的に
  fire-and-forget する場合は理由コメント + `.catch` でエラーを握る (§7)。
- 共有可変状態への並行アクセスは順序保証 / ロックを設計する (§10 の lock も解放
  対象)。競合前提のコードを書かない。

## 12. I/O 境界の契約 — エンコーディングとチャネルを明示 — 必須

**プロセス境界・パイプ・ネットワークでバイト列を扱うときは、エンコーディング /
バイトオーダ / 改行変換 / binary-mode を暗黙に任せず明示する。**

- **データ用ストリームにログを混ぜない。** stdout を生データ (raw フレーム /
  バイナリ / パイプ出力) に使うなら、ログ・診断・バナーは stderr かファイルへ出す。
- バイナリを扱う stdin/stdout は改行変換を止める (Windows は CRLF 変換でバイナリが
  壊れる → binary mode を明示)。
- バイト列の **意味 (pixel format / byte order / 文字コード)** を送受信の双方で
  一致させる。片側だけ変えない。
- 非 ASCII を外部コマンド / シェル経由で渡すときは経路の encoding を確認する
  (ソースファイル自体の文字コードは §17)。

## 13. 外部プロセス / コマンド起動の作法 — 必須 (一部セキュリティ)

**子プロセスや外部コマンドを起動するときは、安全で意図の明確な起動契約を守る。**

- shell を介さず **引数は配列で渡す** (shell 経由のクオート崩れ / インジェクションを
  避ける)。
- **cwd を明示する** (呼び出し元の cwd 依存で挙動が変わらないように)。
- stdin / stdout / stderr の向き先を意図的に決める (inherit / pipe / ignore、§12)。
- **子プロセスの `exit` / `error` を必ず購読し、終了・失敗時のクリーンアップ (§10)
  に繋げる。** 投げっぱなしにしない。

## 14. 秘密情報・個人データの非漏洩 — 必須

**secret と個人データをソース・ログ・エラーに出さない。**

- API キー / トークン / パスワード / 接続文字列をソースに直書きしない。設定は
  Infisical 経由で注入する ([`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md))。`.env`
  はコミットしない。
- secret は long-lived 共有値を避け、必要時発行 + プロセスメモリ保持を既定とする
  (Cernere `project-token` 方式)。
- **ログ・例外メッセージ・スタックトレースに secret / 個人データを出さない。**
  デバッグでダンプする場合もマスキング (`****`) する。
- 個人データの保管そのものは [`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) と
  [`RULE.md`](./RULE.md) §5 に従う (Cernere 単一情報源)。

## 15. ログ

**共有ロガー経由で、レベルを使い分けて記録する。**

- ログは共有ライブラリ **Vestigium** 経由で出す。`console.log` / `println!` の
  直書きを本筋コードに残さない (データ用ストリームへの混入は §12)。
- レベルを使い分ける: `debug` (調査用) / `info` (節目) / `warn` (異常だが継続) /
  `error` (失敗)。本番で `debug` を垂れ流さない。
- 機密・個人データを出さない (§14)。
- 新機能・不安定機能の初期は **過剰ログ → 安定後に撤去** プラクティスに従う
  (仕込み時に撤去 tracker Issue を立てる。スキル `verbose-logging-bootstrap`)。

## 16. 時刻・タイムゾーン・非決定値

**保存・送信は UTC、非決定値は注入可能にする。**

- 時刻の保存・API 送受信は **UTC / ISO 8601** を既定とする。ローカルタイムゾーン
  変換は表示直前のみ。
- epoch number と ISO 文字列を取り違えない (トークン / 署名系で頻出の罠)。
- `Date.now()` / 乱数 / UUID 生成など **非決定値はロジックに直接埋め込まず**、
  引数 or プロバイダで注入してテスト可能にする ([`RULE_TEST.md`](./RULE_TEST.md))。

## 17. コメント・ソース文字コード

**「なぜ」を書く。ソースは UTF-8 に統一する。**

- コメントは **why (意図・背景・トレードオフ)** を書く。コードを読めば分かる
  what の逐語コメントは避ける。
- ソース・コメントは **UTF-8** で保存する。プラットフォーム固有のエンコーディング
  要件に従う (例: PowerShell 5.1 + 非 ASCII の `.ps1` は UTF-8 BOM 必須 / MSVC は
  日本語コメントに `/utf-8`、test target にも個別付与)。
- ランタイムの I/O バイト列の扱いは §12。

## 18. 依存関係の追加

**新規依存は最小限。追加前に必要性・ライセンス・代替を確認する。**

- 既存スタック ([`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md)) で代替できないか
  先に検討する。
- ライセンスを確認する (コピーレフト等を無断で持ち込まない。
  [`common/REVIEW_QUALITY.md`](./common/REVIEW_QUALITY.md) のライセンス遵守と対応)。
- **sibling リポを `{ path = "../OtherRepo/..." }` で直接参照しない** (CI で解決
  失敗)。git dep を既定とし、ローカルは workspace の `[patch]` で path override する。

## 19. null / optional・不変性

- non-null 断言 (`!`) / 強制 unwrap を濫用しない。null / undefined / None は
  明示的に分岐するか早期リターンで処理する (§5)。
- 変数は `const` / イミュータブルを既定とする。再代入が必要な箇所のみ可変にする。
- 共有データを呼び出し側に黙って破壊的変更しない (引数の in-place 変更を隠さない、
  §4 の「嘘をつかない」と対応)。

## 20. TODO / FIXME

- TODO / FIXME を黙って残さない。残す場合は **GitHub Issue 化** し、コメントに
  Issue 番号と撤去条件を書く。
- コメントアウトした旧コードを残さない (履歴は git にある)。

## 21. ユーザ指示が規約に反するとき

ユーザ (または上位の指示) が本規約に反するコードを求めた場合、**黙って
従わず、着手前に 1 文で「これは規約 §N に反します」と指摘し確認を取る。**
ユーザが明示的に上書きを選んだ場合のみ従い、その旨をコメント / PR 説明に残す。

---

## 運用 (規約の所在と強制)

本規約は次の 4 層で運用する。**本文の正本はこの `RULE_CODE.md` のみ** とし、
他層は重複保持せず参照・同期する (ドリフト防止)。

| 層 | 役割 | 実体 |
|----|------|------|
| **正本** | single source of truth | この `AIFormat/RULE_CODE.md` |
| **スキル** | コーディング時に手元へ出す窓口 + 挙動指示 (§21) | 各ワークスペースの `coding-conventions` スキル。本文はここから同期 |
| **メモリ** | 常時コンテキストの短いポインタ (1 行) | 「コード規約は §正本 / スキル参照」のみ。本文は持たない |
| **harness** | 機械的トリップワイヤ + リマインダ注入 | コード編集系ツール (Edit/Write) の PostToolUse フック。ファイル行数 (§2) 超過を警告し、SRP / 分割の要点を再掲する |

- レビュー時は [`common/REVIEW_CODE_QUALITY.md`](./common/REVIEW_CODE_QUALITY.md)
  でこの規約の遵守を確認する (規約=表、レビュー=裏で一致させる)。
- 第 I 部の宣言は [`common/REVIEW_DESIGN.md`](./common/REVIEW_DESIGN.md) で実装との
  一致を確認する。
- 規約を変更したら正本を更新し、各ワークスペースのスキルへ同期する。

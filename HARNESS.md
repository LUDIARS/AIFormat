# HARNESS — 作業開始時に読む入口

LUDIARS のリポジトリで**作業を始める前に最初に読む**文書。
「設計を毎日レビューして直す」のではなく、**書く前にハーネス(枠組み)を揃える**
ことで、設定・コード配置・進め方の判断をその場で正しく着地させる。

> このページは AIFormat 全体への入口かつ要約。各項は詳細ルールの
> **正本へのリンク**を持つ。迷ったらリンク先を読む。リンク先と本ページが
> 食い違う場合は **リンク先(正本)が優先**。

---

## ハーネスの原則 — 強制できるものは記憶に頼らない

ハーネスの目的は、正しい行動を**既定の最短経路**にし、逸脱を構造的に起きにくく
することにある。要は「AI が覚えて守る」ことではなく、**強制できるルールは
仕組み側へ落とす**こと。各ルールは次のどれで担保するかを意識する:

- **記憶** — このページ / メモリ / スキルを参照して守る。柔軟だが**陳腐化に弱い**。
- **フック強制** — 着手前後に harness フックが自動で文脈注入・検査する
  (例: Anatomia `supply→verify` が着手前の着地点・適用ルールを自動供給)。記憶より強い。
- **CI ゲート** — マージ前に機械が落とす(型 / lint / テスト / スキーマ突合)。**最も強い**。

**フック・CI で担保できるものは記憶に頼らない。** 文書が古くなっても仕組みが残る。
新しいルールを足すときは「これは記憶 / フック / CI のどれで守るか」をまず決める。

---

## 0. 使い方

- **作業開始時にこのページを確認する**(セッションごとに 1 回。内容はキャッシュ可)。
- 着手する作業を下記 3 軸で点検してから手を動かす:
  1. **設定をどうするか** → §1
  2. **コードをどう配置するか** → §2
  3. **作業をどう進めるか** → §3
- 個別の深い判断(認証フロー・DB マイグレーション・テスト設計など)は
  §1〜§3 から各 `RULE_*.md` / `FORMAT_*.md` に降りる。
- **ループを閉じる**: 作業終了時は `/mmtask` のタスクを消費し、`session-end` で
  残作業報告・memory 更新まで行う。**開始(読む)と終了(残す)を対にする** —
  どちらか片方だけだとハーネスが回らない。

---

## 1. 設定をどうするか

環境統合(サービス間連携・常駐ツール・dev 起動・自動化エージェント)の
設定とシークレットの扱い。**正本: [`RULE.md`](./RULE.md) §7 /
[`FORMAT_AUTH.md`](./FORMAT_AUTH.md)。**

- **統合設定はファイルで一元管理する。** 接続先 URL / port / 機能フラグ /
  cwd / ログ root / ポーリング間隔などの「環境変数相当」は、起動のたびに env を
  手で渡す運用に依存させず、**リポジトリ管理下の設定ファイルで宣言**する。
  非シークレットの統合設定はコミットしてよい。単一 loader で読み、env override は
  許容するが **既定値はファイル**。「手元だけ env を立てれば動く」状態を残さない。
- **シークレットは平文保存しない。** API キー / トークン / `client_secret` /
  パスワードは平文でファイル・DB・ログ・設定に置かない。検証だけなら salt 付き
  ハッシュ、復号が要るなら AEAD 暗号化(鍵は本体と分離)。設定ファイルには
  **参照名のみ**を置き、実体は `@cernere/env-cli` + Infisical / OS キーチェーン /
  起動時 env 注入で渡す。
- **認証・セッションは Cernere に集約する。** JWT / OAuth refresh token 等の
  責務は各サービスに持たせない([`RULE.md`](./RULE.md) §1 / §5)。
- **設定の保存先**: Claude 設定(settings.json / hooks / skills / state)は
  各ワークスペース配下の `.claude/` に置く(ユーザフォルダ `~/.claude` は
  global preference のみ)。

新しい環境変数を増やしたら設定ファイル(と `.env.example` 相当のテンプレ)に
**必ず反映**する。

---

## 2. コードをどう配置するか

「どこに・どの粒度で・どの依存方向で」コードとドキュメントを置くか。
**正本: [`RULE.md`](./RULE.md) §4 / [`RULE_CODE.md`](./RULE_CODE.md) /
[`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) /
[`FORMAT_SPEC.md`](./FORMAT_SPEC.md)。**

### 2.1 単一責任 + ファイル分割(必須)

- **1 クラス / 1 モジュール / 1 関数 = 1 責任**([`RULE_CODE.md`](./RULE_CODE.md)
  §1)。「○○ かつ △△ をする」は分割の候補。`Manager` / `Util` / `Helper` に
  雑多を詰め込まない(God Class 禁止)。
- **1 ファイル 1 責務**([`RULE_CODE.md`](./RULE_CODE.md) §2)。分割の判断軸は
  **責任であって行数ではない**。単一責任なら 1000 行超も可、無関係 2 責務なら
  100 行でも割る。**着手前に `/coding-conventions` スキルを参照。**

### 2.2 コア / モジュール分離

- **コアは最小実装のみ**(認証・セッション・基本データモデル)。便利機能を
  コアに混ぜない。**拡張はモジュール化し着脱可能に**する。コアはモジュールに
  依存しない([`RULE.md`](./RULE.md) §4.1)。

### 2.3 データの置き場所

- **個人データは各サービス DB に持たない。Cernere が単一情報源**
  ([`RULE.md`](./RULE.md) §5)。氏名 / email / role / トークン等は `id` を
  FK アンカーとしてのみ保持し、Profile API で取得する。
- DB / 永続データの種別分類・保存先・保護要否は
  [`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) に従い `spec/data/` に明文化。
- マイグレーションは番号連番 + 冪等(`IF NOT EXISTS`)、`DROP TABLE` /
  `DROP COLUMN` / 型変更は禁止([`RULE.md`](./RULE.md) §2)。

### 2.4 ドキュメントの置き場所(spec/)

- 設計仕様はルート `spec/` の **6 分類**
  (`data` / `feature` / `interface` / `plan` / `setup` / `test`)に必ず格納する
  ([`FORMAT_SPEC.md`](./FORMAT_SPEC.md))。`plan/` 以外の 5 分類は
  「あってしかるべき」常設ドキュメント。
- 実装に入る前に、第 I 部の 3 点(採用アーキ・目的と重視点・設計判断)を
  `spec/` に宣言してから書く([`RULE_CODE.md`](./RULE_CODE.md) 第 I 部)。

### 2.5 技術スタック

- Web サービスは **TypeScript**、ネイティブ機能を持つデスクトップは
  **Rust + Tauri**。既定どおりなら「既定に従う」で良く、外す場合は理由を
  `spec/` に書く([`RULE.md`](./RULE.md) §4.2 / [`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md))。

---

## 3. 作業をどう進めるか

「どの順序・どの粒度・どこまで自走で」作るか。デイリーレビューに頼らず、
**進め方そのものをハーネスとして固定する**。

### 3.1 フルセットで作る(No-MVP)

- AI が実装する機能は **MVP / 最小縦切りを作らない**。設計済みの機能を
  **フルセットで実装 → 配線 → テスト**まで通す。タスク分解は全機能を
  feature 単位・SRP で列挙する(「まず最小から」と縮めない)。
  → スキル `full-set-implementation`。
- 「あと一押しで直りそう」で同一実装を直し続けない。**同一目的・同一
  アプローチで 3 回失敗したら実装を捨てて方式から作り直す**(3 アウト制)。
  → スキル `three-out-rule`。

### 3.2 一気通貫で自走する

- **承認ゲート無しで 調査 → 設計 → 実装 まで一気通貫**で進め、設計書
  (`spec/plan/`)を残して事後報告する。フェーズ分割で逐次承認を取らない。
  → スキル `prototyping-flow` / メモリ `autonomous-impl-flow`。
- **例外: 破壊的・外向きの操作は事前確認する**(削除 / 上書き / 公開 /
  外部送信)。この自走ルールは破壊的操作の事前確認を免除しない。
- **指示の範囲だけ作る。** 依頼物の種類・形式・範囲だけを作り、便利
  スクリプトや「ついで」を勝手に足さない。規約に反する指示は着手前に指摘する。

### 3.3 ブランチ → PR → 自動マージ

- **main 直 push 禁止。全変更は feat / fix ブランチ + PR 経由。**
- **編集は worktree 経由**で行う(メインチェックアウトでブランチを切り替えない。
  並行セッション・自動化が同一 working tree を壊すため)。worktree は
  作る前に `origin/main` を fetch して最新 base で切る。
  → [`RULE.md`](./RULE.md) §6 / スキル `worktree-hygiene`。
- **AI 実装は 1 PR 集約**(フェーズ分割しない)。
- **Ars 配下は自動マージ**(squash + delete)→ クリーンアップ(worktree 撤去 +
  prune + main 同期)→ **同セッションの残タスクへ継続**。**CI が red の時だけ
  止める**(無 CI はマージ可)。
- マージ完了は origin 実体で裏取りする(`gh` 表示だけで判断しない)。

### 3.4 検証は実経路で裏取りする

- 委譲(Sonnet 等)の「ビルド green / テスト green」報告を鵜呑みにしない。
  **フラグ隔離 / 条件コンパイルされた ON 経路、実 DB 書き込み経路、consumer の
  リンクは自分で実走して裏取り**する。fake-dep / 注入テストは実 SQL 経路を
  検証しない。詳細は [`RULE_TEST.md`](./RULE_TEST.md)。

### 3.5 選択肢の評価軸

- 優先順位・選択肢比較・設計判断は、日数 / 金額ではなく
  **(1) AI 学習量 (2) 作業コスト (3) 解決度 (4) 主目的との一致度** で評価する
  (人間が手を動かさない環境のため日数・金額はトレードオフにならない)。
  → スキル `decision-metrics`。

### 3.6 完了の定義 (Definition of Done)

自走(§3.2)の終点を次で固定する。**全部を満たすまで「完了」と報告しない:**

1. 設計済み機能を**フルセットで実装 + 配線**した(§3.1。MVP で止めない)。
2. **テストが green**(該当アーキのテスト種別 — [`RULE_TEST.md`](./RULE_TEST.md))。
3. **実経路で裏取り**した(§3.4。ビルド green の自己申告で代替しない)。
4. branch → PR → マージ → クリーンアップまで通し、**origin 実体で確認**した(§3.3)。
5. 残タスク・学びを**記録**した(§0 のループ閉じ)。

### 3.7 トレースを残す(観測可能性)

- 新機能・不安定機能は初期に**過剰レベルでログを仕込み**、安定後に撤去 PR で
  落とす(撤去条件は GitHub Issue で tracker 化)。→ スキル `verbose-logging-bootstrap`。
- LLM コスト等の横断計装は集約ハブ(Anatomia)へ送る。「動いたか」だけでなく
  「いくらかかったか / どこで詰まったか」を残せる状態にしておく。

### 3.8 委譲とモデル適性

- 実装の手を委譲するときは適性で割り当てる: **網羅列挙は Haiku 総当たり + 検証、
  精密検索は構造的見落としに注意、コード書きは Codex 委託**。
  → スキル `codex-delegate` / メモリ `model-task-fit`。
- 委譲は cwd を明示し、参照は `origin/main` 最新を渡す(stale ローカル main を
  掴ませない)。**結果は §3.4 で必ず実経路裏取り**する。

---

## 4. 既知の地雷 (実害が出た操作)

抽象的な「破壊的操作は確認」(§3.2)より、**具体パターンの方がハーネスとして強い**。
以下は過去に実害が出た操作。該当したら手を止めて確認する。

- **`git reset --hard` で main 同期しない。** 共有作業ツリーの別セッション
  未コミット変更を無告知破壊する。同期は `fetch` + `merge --ff-only`、事前に
  `status` clean 確認。消したら `gc` 禁止 + `fsck` で dangling blob 復元。
- **稼働中の SQLite を `cp` / `mv` で差し替えない。** WAL で開いた DB は破損する。
  反映は SQLite 書き込み経由。差し替えはプロセス停止確認後のみ(`-wal` / `-shm`
  busy = 生接続の合図)。
- **`rebase` が untracked 衝突で abort したら `git stash push -u`。**
  `mv backup/` + `rm -rf` は地雷(実害あり)。
- **`gh pr merge` は worktree から実行しない。** main 二重 checkout 衝突で失敗。
  worktree 撤去 → リポ dir(`<workspace-root>/<repo>`)から実行。
- **委譲の「green」報告でマージしない**(§3.4 と同義 — 地雷として再掲)。

> 環境固有の罠(Windows: `.ps1` は UTF-8 BOM 必須 / `.bat` は ASCII のみ /
> `TZ=Asia/Tokyo` は git-bash で UTC 化)は各メモリ・スキルを参照。

---

## 5. 関連ドキュメント

| 軸 | 正本 |
|----|------|
| 基盤(認証 / DB / 個人データ / worktree / 設定) | [`RULE.md`](./RULE.md) |
| 技術スタック選定 | [`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md) |
| コーディング(SRP / 分割 / レイヤー) | [`RULE_CODE.md`](./RULE_CODE.md) |
| データスキーマ | [`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) |
| テスト | [`RULE_TEST.md`](./RULE_TEST.md) |
| SRE・運用 | [`RULE_SRE.md`](./RULE_SRE.md) |
| spec 記法(6 分類) | [`FORMAT_SPEC.md`](./FORMAT_SPEC.md) |
| 認証記法 | [`FORMAT_AUTH.md`](./FORMAT_AUTH.md) |
| レビュー(見るとき) | [`REVIEW.md`](./REVIEW.md) |

> **レビュー(`REVIEW.md` / `common/` / `web/` `local-app/` `game/`)は
> 「書いた後に見るとき」の参照として残す。** ハーネス(本ページ)は
> 「書く前に揃えるとき」の入口。両者は別軸。

# データスキーマ規約 (Data Schema)

LUDIARS 全リポジトリ共通の **データの分類・保存先・保護方針の規約**。

各サービス / アプリは、自分が扱う **すべてのデータについて「種類・保存先・保護
要否」を `spec/` 以下に明文化する**。コード上の取り扱い (secret / 個人データを
ログに出さない等) は [`RULE_CODE.md`](./RULE_CODE.md) §14、認証・個人データの
基盤ルールは [`RULE.md`](./RULE.md) §5 を参照。

> この文書は「何を・どこに・どう守って持つか」を **設計時に宣言させる** ための
> 規約。実際の保管禁止ルールの正本は [`RULE.md`](./RULE.md) §5。

---

## 1. データの種類を分類する

扱う各データ (テーブル / コレクション / ローカルファイル / KVS エントリ) を
最低限 **マスタデータ / ユーザデータ** に分類する。

| 種類 | 定義 | 例 |
|------|------|-----|
| **マスタデータ (master)** | 変更頻度が低く参照中心。権威ソース (誰が更新するか) が決まっている | 施設一覧 / 商品マスタ / カリキュラム定義 / ゲームのアセット定義 |
| **ユーザデータ (user)** | ユーザの行動・入力に起因して生成・更新される | 予約 / ブックマーク / 日記 / セーブデータ / 行動ログ |

- どちらとも言い切れないデータは、**どちらに寄せるか + 理由**を書く。
- マスタデータは **権威ソース (single source of truth) がどこか** を明記する
  (例: カリキュラムは Calicula が権威、施設予約は Actio が権威)。

## 2. ユーザデータの保存先を明記する

ユーザデータは **どこに保存するか** を必ず宣言する。

| 保存先 | 用途 | 備考 |
|--------|------|------|
| **Cernere** | 個人データ (氏名 / email / role / 認証トークン等) | **自前 DB に持たない。`id` を FK アンカーとしてのみ保持** ([`RULE.md`](./RULE.md) §5) |
| **サービス自前 DB (PostgreSQL)** | そのサービス固有のドメインデータ | Drizzle ORM / リポジトリ層経由 ([`RULE_TECH_STACK.md`](./RULE_TECH_STACK.md)) |
| **ローカル (SQLite / ファイル)** | ローカルアプリのローカル完結データ | 端末内に閉じる。同期する場合は経路を明記 |
| **オブジェクトストレージ (MinIO 等)** | 画像 / 添付 / 大きなバイナリ | 必要なサービスのみ |

- **個人データ** (本人の属性とみなせるもの) は原則 Cernere 単一情報源とし、
  自前 DB に複製しない。サービス固有で Cernere に存在しないドメインフィールド
  (例: Schedula の学科、Calendar 連携 nonce) は各サービス DB に保存してよい
  ([`RULE.md`](./RULE.md) §5.4)。
- 保存先が複数にまたがる場合は **どれが権威で、どれがキャッシュか** を書く
  (キャッシュには TTL を明記)。

## 3. 保護が必要かどうかを判定する

各ユーザデータについて **保護要否** を判定し、必要なものは保護方法を明記する。

- **保護が必要 = 個人情報 / 機微情報 / 位置情報 / 認証情報 / 本人にしか見せない
  内容** など。
- 保護が必要なデータは、以下のうち該当する対策を spec に書く:
  - アクセス制御 (誰が読めるか。Cernere の認可モデルに従う)
  - 転送経路 (センシティブな長期接続 / GPS / MQTT は Tailscale 等で閉じる)
  - 保管時の扱い (暗号化 / マスキング / 保持期間・削除方針)
  - ログ・エラーに出さない ([`RULE_CODE.md`](./RULE_CODE.md) §14)
- 保護不要と判断したものも「保護不要」と明記する (判定漏れと区別するため)。

## 4. `spec/` 以下への整備義務

上記 1〜3 を **`spec/data-schema.md`** (またはそれに準ずる場所) に表で残す。

- 列の目安: `データ名 | 種類(master/user) | 権威ソース | 保存先 | 保護要否 | 保護方法`。
- **新規テーブル / カラム / ローカルデータを追加したら同じ PR で更新する。**
  DB マイグレーション ([`RULE.md`](./RULE.md) §2) と歩調を合わせる。
- レビューは [`common/REVIEW_DESIGN.md`](./common/REVIEW_DESIGN.md) /
  スタイル別の脆弱性・プライバシーレビュー (`local-app/REVIEW_PRIVACY_POLICY.md`
  等) でこの整備状況を確認する。

---

## なぜ？

- 個人データを各サービスが勝手に持つと、Cernere の opt-out / 削除と整合が取れず
  GDPR 等への対応が崩れる ([`RULE.md`](./RULE.md) §5.5)。
- 「どのデータが保護対象か」を設計時に宣言させることで、実装・レビュー・運用
  (SRE) が同じ前提で判断できる。
- マスタ / ユーザの分類と権威ソースの明記は、サービス間連携時の二重管理・
  不整合を防ぐ。

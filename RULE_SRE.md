# SRE・運用規約 (SRE / Operations)

LUDIARS 全リポジトリ共通の **運用にあたって気をつける観点** を、サービスの種類別に
定める。あわせて **異常を検知したときの Issue 報告** の運用を定める。

可観測性の基盤は Vestigium (ログ収集) + Concordia (横断監視 / error-detection)。
コード側のログ規約は [`RULE_CODE.md`](./RULE_CODE.md) §15。

---

## 1. サービス種別ごとの運用 watchpoint

各リポジトリは、自分の種別に該当する観点を `spec/` 以下 (例: `spec/operations.md`)
に運用方針として残す。

### 1.1 Web サービス
- **可用性 / ヘルスチェック**: 死活エンドポイント、起動失敗の検知。
- **依存の外部化**: PostgreSQL / Redis は共有インフラ前提。接続断・TTL 失効
  (Redis 7日) からの復帰。
- **マイグレーション**: 冪等に流れること ([`RULE.md`](./RULE.md) §2)。CREATE INDEX は
  ALTER の後 (既存 DB で no such column を出さない)。
- **レート制限 / 濫用対策**、認証境界の監視 (Cernere)。
- **外部アクセス制御の責務分離**: Cloudflare Access / Tunnel などの外部ゲートウェイが
  認証・公開境界を管理する場合、その境界のポリシーは外部ゲートウェイを正本とする。一方で、
  アプリケーションの `Host` / `Origin` 検証は Host header 攻撃、CORS、CSRF などの
  プロトコル上の防御として維持する。プロキシ経由の正規 Host も許可するよう設定し、追加の
  アクセス制御を置く場合は外部境界と重複しない責務を spec に明記して回帰テストを置く。
- **バックアップ / リストア** 方針。
- **ポート競合**: ホスト port は PORT-MAP に従い、既知の squat (Dropbox 17500 等)
  を避ける。

### 1.2 ローカルアプリ (デスクトップ / CLI / 常駐)
- **クラッシュ復帰 / 自動再起動**、多重起動・古いプロセス残留の検知。
- **ローカルデータ破損対策**: SQLite migration の immutability、書き込み中断耐性。
- **オフライン動作** とオンライン復帰時の同期。
- **自動更新** とロールバック。
- **センシティブ情報の保護** ([`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) §3、
  GPS / 長期 WS / MQTT は Tailscale で閉じる)。
- 常駐プロセスのリソースリーク (§ コード §10) の長時間監視。

### 1.3 ゲーム (クライアント)
- **フレーム budget / メモリ / GC スパイク** の監視。
- **セーブデータ破損対策** とバックアップ世代。
- **アセットロード失敗時のフォールバック** (欠損アセットでクラッシュさせない)。
- プラットフォーム別の互換 (CRT / ビルド構成。例: KS は Release 必須)。

### 1.4 共有ライブラリ / 基盤層
- **後方互換**: 公開 API の破壊的変更は consumer へ周知し、リンク/ビルドを確認
  ([`RULE_TEST.md`](./RULE_TEST.md) consumer テスト)。
- バージョニングと変更履歴。

### 1.5 P2P / リアルタイム通信
- NAT 越え / fallback 経路 (IPv4 fallback、dual-stack の罠)、bootstrap 経路と
  データプレーンの分離 (Cloudflare Tunnel は UDP を通さない等)。
- 接続の生存監視 (ping/pong)、再接続。

---

## 2. ヘルスチェックエンドポイント (`/api/health`)

HTTP を待ち受けるサーバは **死活エンドポイントを 1 本、決まった形で公開する**。
Excubitor / Concordia / Corpus hub がサービス横断で同じ形を叩けることが目的なので、
パスと最小フィールドを**サービスごとに変えない**。

### 2.1 契約

- **パス**: `/api/health`。**認証不要** (auth middleware より前に登録する)。
- **メソッド**: `GET`。**副作用を持たない**。
- **必須フィールド**:

| フィールド | 型 | 内容 |
|---|---|---|
| `ok` | boolean | 自プロセスが応答可能なら `true` |
| `service` | string | サービス識別子。Excubitor catalog の `code` と一致させる |
| `version` | string | **いま走っているプロセスが名乗る版** (§2.3) |

- 任意フィールド (`port` / `modules` 等) を足してよいが、§2.2 の軽さを破らない範囲に限る。
- **認証不要なので、内部情報を載せない。** 返すのは上記の最小フィールドまで。
  依存先ホスト名 / 接続文字列 / 資格情報の有無 / 内部パス / スタックトレースを
  health に出さない。インターネットに面するサービスでは、`/api/health` を
  公開面に晒さない (LAN・監視ネットワーク限定、または reverse proxy で制限) こと。

### 2.2 health を重くしない (必須)

health は監視から高頻度に叩かれる。**1 リクエストで外部 I/O を発生させない。**

- **禁止**: DB クエリ / downstream サービスへの HTTP / ファイル読み込み /
  外部プロセス起動 / ロック取得。
- 判定基準は単純で、**ハンドラが同期のまま応答を返せること**。`await` が要る形に
  なった時点で設計を疑う。
- 依存の生死を出したいときは、(a) バックグラウンドで定期更新したキャッシュ値を
  読むだけにするか、(b) **別エンドポイントへ分ける** (`/api/readiness` 等)。
  「依存が落ちていると health のレスポンスが遅くなる」構造は、監視のタイムアウトを
  誘発して**自分が落ちたように見せる**ので特に避ける。

### 2.3 version は「走っているもの」を名乗る

`version` は**ディスクに何が置かれているか**ではなく、**このプロセスが何を読み込んで
起動したか**を表す。Excubitor は `package.json` をファイルとして読んだ値
(`package_version`) を別途持っているので、両者を突き合わせれば
「dist を再ビルドせずに再起動した」「長命ワーカーが旧コードを掴んだまま」といった
**反映したつもり**を機械的に検出できる。health に version を載せる主目的はこれ。

- 解決順は **`EXCUBITOR_SERVICE_VERSION` → サービス固有 env → `npm_package_version` →
  `'unknown'`**。先頭が最優先。
- `EXCUBITOR_SERVICE_VERSION` は **Excubitor が起動時に注入する版**
  (`Excubitor/src/process/service-version.ts`, SPEC-SERVICE-RUNTIME-VERSION)。catalog の
  `cwd` から `package.json` の version を、無ければ `0.0.0+<git hash>` を解決して子プロセスへ
  渡している。これをそのまま名乗り返せば、Excubitor は**自分が注入した値**と**いまディスクに
  ある値**を同じ土俵で比較でき、突き合わせが厳密になる。
- **バージョン文字列をソースにハードコードしない。** package.json を bump した後も
  古い値を名乗り続け、上の突き合わせが無意味になる。フォールバックは `'unknown'` にして
  「取れなかった」ことを見えるようにする。

---

## 3. Issue 報告

運用中に検知した異常 / 障害 / 不安定挙動は **GitHub Issue に記録** して追跡する。

- **検知したら起票する**: 監視 (Concordia / error-watch) や手動で見つけた異常は、
  握って終わらせず Issue にする。
- **起票内容**: 重大度ラベル / 事象と再現手順 / 影響範囲 / 関連ログ (Vestigium)
  へのポインタ / 暫定対応の有無。
- **撤去・暫定対応の tracker**: 過剰ログや一時 workaround を入れたら、撤去条件を
  Issue に明記して追跡する (スキル `verbose-logging-bootstrap` と同じ運用、
  [`RULE_CODE.md`](./RULE_CODE.md) §20)。
- 自動修正 (AUTOFIX) の対象範囲・対象外は [`REVIEW.md`](./REVIEW.md) に従う。
  設計判断 / 大型実装を要するものは Issue に残して人間に委ねる。

---

## なぜ？

- 死活エンドポイントの**パスと最小フィールドを固定**することで、監視側 (Excubitor /
  Concordia) がサービスごとの差異を吸収するコードを持たずに済む。version を同じ場所で
  返させるのは、**ディスク上の版と実際に走っている版の食い違い**という「反映したつもり」を
  検出可能にするため。
- health を軽く保つのは、依存の遅延が自分の死活判定に伝播して**落ちていないのに落ちて
  見える**状態を作らないため。死活 (liveness) と依存の準備状況 (readiness) は別の問い。
- 「気をつける観点」をサービス種別で固定化することで、Web の運用知をゲームに
  押し付ける/ローカルアプリにクラウド SRE を求める、といったミスマッチを避ける。
- 異常を Issue に集約することで、検知 → 原因 → 修正 → 撤去のループが追跡可能に
  なり、同じ障害の再発と「直したつもり」を防ぐ。

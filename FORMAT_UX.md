# UX 定義書 — プロダクトが解決する問題と勝ちの条件

この書式は Anatomia と Praeforma が共通で参照する UX 定義書の正本。
UX は画面の外観に限らず、利用者が目的を達成するまでの体験を指す。
CLI、API、ゲーム、ライブラリ、制作物でも、誰にどの価値を渡すかを記載する。

## 1. 正本と粒度

- 各プロダクトの `spec/ux/product.md` に、プロダクト全体が解決する問題と勝ちを記載する。
- コアドメインごとに `spec/ux/<domain>.md` を作り、その勝ちへの貢献を記載する。
  これは画面仕様 (`spec/feature/`) やドメイン所属 (`spec/domains/`) と別の文書。
- コアドメインの UX は `product_ux` でプロダクト UX を参照する。全体の目的は複写しない。
- ドメイン定義の既存 `specRefs` に UX 文書パスを追加する。共通ライブラリ等を機械的に
  コア扱いせず、コア / 支援 / 汎用の分類理由を UX 本文に記載する。
- 内容の所有者は対象プロダクト。Anatomia は紐付けと解析、Praeforma は体験設計で参照する。
  どちらの DB / キャッシュ / 生成画面にも別の編集可能な正本を作らない。

## 2. 最小メタデータ

既存 OKF と共存するため `type: feature` を維持し、`ux_definition: 1` で識別する。
`id` は内容やファイル名の変更でも保持する。`service` は登録されたプロダクト識別子。
`domain` はコアドメイン文書では `spec/domains/` の `name` と一致させる。

```yaml
---
title: タスク完了の UX
type: feature
ux_definition: 1
id: UX-TASK-COMPLETION
service: task-example
domain: task-completion
ux_scope: core-domain
product_ux: spec/ux/product.md
status: draft
owner: product-owner
---
```

プロダクト文書は `ux_scope: product` とし `domain` / `product_ux` を省略する。
`owner` は役割名でよい。個人情報を要求しない。
`product_ux` / `specRefs` は対象リポジトリ内の相対パス。外への traversal や symlink を許可しない。

## 3. 必須の本文

### 誰の、どの状況の問題か

対象利用者、利用場面、今困っていること、現状の代替手段、その不利益を書く。
「管理する」「便利にする」だけで終えず、実装前に利用者と確認できる具体例を添える。

### このプロダクトが何を解決するか

利用前 → 利用後の変化を 1〜2 文で書く。コアドメイン文書ではプロダクト UX を参照し、
その中で自分が担う価値・持たない責務を明示する。機能リストを目的の代わりにしない。

### 何を勝ちとするか

安定した ID (`UX-W1` 等) を各条件に付け、次を表で記載する。

| ID | 利用者に起きる望ましい変化 | 判断方法・条件 | 証拠 | 現状 |
|---|---|---|---|---|
| UX-W1 | 未達の仕事を完了と誤認せず、次に必要な行動が分かる | 未達項目の提示と、未達時に完了しないことを確認 | 操作シナリオ + 状態遷移の契約 | 未測定 |

数値化できるものは対象母集団・期間・目標・取得方法も書く。数値化しづらい体験は
評価者・シナリオ・観察基準を記載する。目標値、仮説、測定済みの実績を混同しない。
「実装した」「例外が出ない」「テストが通る」はそれだけではプロダクトの勝ちにならない。

### 主要シナリオと失敗からの回復

きっかけ → 利用者の行動 → 応答 → 到達する状態を記載する。
不足入力・権限不足・外部停止・中断時に何を伝え、どう再開できるかも定義する。
UI が無い製品では API 利用者、組込み側、制作者等の操作に置き換える。

### 守る制約と優先順位

整合性・待ち時間・プライバシー・資源量・アクセシビリティ等から該当するものを選び、
勝ちのためにも破ってはいけない条件とトレードオフの優先順位を書く。
UX から導く業務不変条件はドメイン仕様へ、計測予算はテスト/SRE 仕様へリンクする。

### 対応関係・非目標・未決事項

`勝ち ID → シナリオ → ドメインの不変条件 → 契約 ID / 検証 / 人間レビュー` を記載する。
関数契約で保証できない体験を契約の合格で代用しない。非目標と未決事項には判断者を付ける。

## 4. 下書き、承認、変更

- LLM は `draft` を作る。人間の確認なしに `approved` を付けない。
- 承認時には文書 ID とその内容ハッシュ、承認者の役割、承認記録を紐付ける。
  `status: approved` の自己記載だけを機械的な承認証跡にしない。
- 問題・勝ち・制約が変わったら承認を再評価する。変更前の承認・観測を使い回さない。
- Praeforma の会話による修正は変更提案として扱い、対象リポジトリへの変更とレビューを経て反映する。
- 未承認でも探索・下書きは行える。承認済み成果の受け入れ条件として使う時は、
  必要な承認と検証が揃わなければ未確認として止める。

## 5. 参照するシステムの責務

Anatomia と Praeforma の共通参照契約は
[core-domain-ux-consumers.md](spec/interface/core-domain-ux-consumers.md)。
対応サンプルは [core-domain-task](examples/core-domain-task/README.md)。
書式の存在、読み取り対応、強制ゲートの稼働は別々の導入状態として記録する。

# UX 定義と契約サンプル

## 目的

neco 2026-09-07: DDD と契約観測のサンプルを作り、コアドメインの UX 定義書を
Anatomia / Praeforma が共通参照する資料として設ける。

## 完了条件

- 共通 UX 書式、読み取り側契約、サンプルの UX とドメイン定義を同梱する。
- 同期関数の参照ラッパー、版固定の証跡判定、正常/異常/故障の実行例とテストを同梱する。
- 読取機能の本番導入やランタイム接続が未実施であることを明記する。
- 本セッションではテスト・サービス起動・マージを行わず、PR 提出で停止する。

## 変更範囲

AIFormat の規約・レビュー項目、`examples/core-domain-task/` と関連 spec。
再利用調査: Anatomia の OKF `type: feature` と既存 `specRefs` を使用する。
Augur の covered/violated/uncovered と契約 ID の考え方を引き継ぐが、手元 Lapilli に
`contract` export がないため未確認の API を呼ぶ形にはしない。サンプルランタイムは
同期のみの教材として分離し、製品共通ライブラリとして導入しない。

## 検証記録

テスト・サンプル実行・型検査・ビルド: 未実施 (ユーザー指示)。差分とコードは静的に読み直す。
`git diff --check`: 実施、空白エラーなし。テストの合格結果ではない。

## 後続実装

- Anatomia: UX 読み取りと plan / domain view の供給、版・承認の診断。
- Praeforma: UX 射影の表示・シナリオとの対応・Cc 委託への参照添付。
- Lapilli / Augur / Cc: 実ランタイムの接続、版付き証跡と必須契約を本番経路へ導入。
- Codex: 管理フックと Revisor ゲートの接続。テスト禁止時は未検証で保持する。
- 別言語・リアルタイムコンテンツ: 同じ UX / 証跡モデルと専用アダプターで評価する。

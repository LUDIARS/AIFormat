# Spec 生成フォーマット

各プロジェクトはルートに `spec/` フォルダを持ち、プロジェクトの設計仕様を管理する。
下記 9 つを標準分類として推奨するが、分類は固定しない。TaskWorkflow の `tasks/` など、
プロジェクト固有の正本を表すフォルダや `spec/` 直下の文書を追加してよい。

---

## 1. ディレクトリ構成

```
spec/
├── data/         # データスキーマ
├── domains/      # ドメイン定義（コードの帰属先。Anatomia が読む正本）
├── faq/          # 調査結果・よくある質問・設計背景の知識ベース
├── feature/      # 機能概要（1 ファイル 1 機能）
├── interface/    # API・外部連携のドキュメント
├── knowledge/    # 蓄積型ナレッジ（種別サブフォルダに格納）
│   └── problems/ #   発生した問題・障害の記録
├── plan/         # 設計書（実装計画書）
├── setup/        # セットアップ
└── test/         # テスト
```

各フォルダはプロジェクトの構成に応じて必要なものを作る。ただし
**`plan/`・`faq/`・`knowledge/`・`domains/` 以外の 5 分類は標準の必須候補**として扱い、未配置なら
`check-spec-structure.mjs` が warning を出す。欠落だけでローカル検証は失敗させない
（→ [§11 ドキュメント充実度](#11-ドキュメント充実度)）。

---

## 2. `data/` — データスキーマ

DB / 永続データを持つプロジェクトのスキーマを記載する。種別分類・保存先・
保護要否は [`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) に従う。

### 記載内容
- テーブル / コレクション定義（カラム名・型・制約・デフォルト値）
- リレーション（外部キー・参照関係）
- インデックス / ENUM / カスタム型
- マイグレーション方針（[`RULE.md`](./RULE.md) §2 の冪等性）

## 3. `domains/` — ドメイン定義（コードの帰属先）

そのプロジェクトのコードが **どの責務に属するか** を宣言する。1 ドメイン =
1 ファイル（`<domain>.domain.json`）で、Anatomia がこのフォルダを正本として読み、
変更されたコードの着地ドメインを判定する。

`feature/` が「利用者から見た機能」を語るのに対し、`domains/` は
**コードの帰属**を語る。両者は 1 対 1 とは限らない（1 ドメインが複数機能を支える、
逆に 1 機能が複数ドメインにまたがる、はいずれも正常）。

### 記載内容
- `name` — ドメイン名（コードの責務を表す）
- `description` — そのドメインが何を所有し、何を所有しないか
- `membership` — 帰属するパスの正規表現（`pathPattern`）/ 記号名（`namePattern`）。
  実装とテストは対で宣言する
- `presetRules` / `templateRules` — 守らせたい規約があるときだけ書く（任意）

### 注意
- **全域を被覆する**。どのドメインにも属さないファイルが残ると、そこを触った PR が
  「対象ドメインが無い」で止まる（止まるのは作った本人の PR とは限らない）。
- 置き場はこの 1 か所に限る。`.anatomia/domains/` はローカル状態、
  `spec/data/ontology/` は派生成果物であり、いずれも正本ではない。
- JSON は手で編集する。読み書きで整形し直すと正規表現のエスケープが壊れやすい。

## 4. `feature/` — 機能概要（1 ファイル 1 機能）

利用者から見た「機能」単位で **1 機能 = 1 ファイル** で記載する
（例: `feature/bookmark-save.md`, `feature/mfa.md`）。サービスが何を提供するかの
一次情報。

### 記載内容
- 機能の目的・ユーザーストーリー（誰が何のために使うか）
- 振る舞い（入力 → 処理 → 出力）と主要な状態遷移
- 制約・前提・既知の制限
- 関連する `data/` `interface/` への参照リンク

## 5. `interface/` — API・外部連携

サービスの外部接点を記載する。内部実装ではなく **境界（contract）** を書く。

### 記載内容
- REST / WebSocket / gRPC / IPC のエンドポイント（メソッド・パス・req/res・エラー）
- 認証・認可境界（[`RULE.md`](./RULE.md) §1 / Cernere）
- 外部サービス連携（OAuth / MQTT / S3 / 他 LUDIARS サービス等）の仕様と前提
- イベント / Webhook のペイロード定義

## 6. `plan/` — 設計書（実装計画書）

ある実装に着手する前の **設計・実装計画**。フェーズ・対象ファイル・段取り・
トレードオフを残す。実装完了後は陳腐化しうる **作業ドキュメント** であり、
恒久ドキュメント（data/feature/interface/setup/test）とは性質が異なる。

### 記載内容
- 背景・ゴール・非ゴール
- 設計判断と選択肢の比較
- 実装ステップ・影響範囲・ロールバック方針

> `plan/` は「あってしかるべき」常設ドキュメントではない（実装の都度生まれ、
> 役目を終える）。このため **ドキュメント充実度の評価対象から除外する**。

## 7. `faq/` — 調査結果・よくある質問

調査・質問・トラブルシューティングが発生したときに残す知識ベース。
セッション起動時には読まず、必要になったときに参照する。

### 記載内容
- **investigation**: 「〇〇の仕組みを追った」調査ログ
- **qa**: 「〇〇と〇〇の違いは」よくある質問
- **design**: 設計判断の背景・廃案になった選択肢

エラー・障害など **発生した問題の記録** は `faq/` ではなく
[`knowledge/problems/`](#7-knowledge--蓄積型ナレッジ) に蓄積する（→ §7）。

### フォーマット

```markdown
---
tags: [tag1, tag2]
date: YYYY-MM-DD
kind: investigation | qa | design
---

# Q: <質問 / テーマ>
...
```

`spec/faq/index.md` にタグ索引と新着一覧を維持して検索効率を保つ。

> `faq/` は随時追記型であり「あってしかるべき」常設ドキュメントではない。
> このため **ドキュメント充実度の評価対象から除外する**。

## 8. `knowledge/` — 蓄積型ナレッジ

`faq/` と同じく随時追記型の知識ベース。`faq/` が「質問・調査への回答」を残すのに
対し、`knowledge/` は **発生した事実の記録** を種別サブフォルダに蓄積する。
セッション起動時には読まず、必要になったときに参照する。

蓄積量が増える場合は、`knowledge/` 直下へ並べ続けず種別サブフォルダに分けることを推奨する。
小規模な正本や索引は直下に置いてよい。

### サブフォルダ

- **`problems/`** — 発生した問題・障害・不具合の記録。**1 問題 = 1 ファイル** で
  症状・原因・解決策・再発防止を書く。再発防止は HARNESS の原則に従い
  「記憶 / フック / CI のどれに落としたか」まで残す。

種別が増えたらサブフォルダを追加し、プロジェクト側の仕様に役割を記載する。

### フォーマット（`problems/`）

```markdown
---
tags: [tag1, tag2]
date: YYYY-MM-DD
---

# <問題の要約>

- **症状**: 何が起きたか（エラーメッセージ・影響範囲）
- **原因**: 根本原因
- **解決策**: どう直したか
- **再発防止**: どのルール / フック / CI に落としたか（落とせないなら理由）
```

`spec/knowledge/index.md` にタグ索引と新着一覧を維持して検索効率を保つ。

> `knowledge/` は随時追記型であり「あってしかるべき」常設ドキュメントではない。
> このため **ドキュメント充実度の評価対象から除外する**。

## 9. `setup/` — セットアップ


開発・運用の立ち上げ手順を記載する。

### 記載内容
- 必要な前提（言語ランタイム・バージョン・外部サービス）
- 環境変数 / シークレットの一覧と取得元
- ローカル起動 / docker / マイグレーション適用手順
- デプロイ / リリース手順（あれば）

## 10. `test/` — テスト

テスト設計を記載する。種別（ビルドチェック / ユニット / smoke / 統合 / E2E 等）
ごとに「何を担保するか」を書く。詳細方針は [`RULE_TEST.md`](./RULE_TEST.md)。

### 記載内容
- 種別ごとの目的・対象・ローカルでの実行方法・リリース判定での扱い・現状・やること
- アーキ（サービス種別）から逆算した「何を充実とみなすか」（RULE_TEST §2/§3）

---

## 11. ドキュメント充実度

**`plan/`・`faq/`・`knowledge/`・`domains/` を除く 5 分類（`data` / `feature` / `interface` / `setup` / `test`）は、
どのプロジェクトにも本来あるべきドキュメントである。** これらがどれだけ揃い、
実装と乖離せず維持されているかを **ドキュメント充実度** という指標で評価する。

- **対象**: `data` / `feature` / `interface` / `setup` / `test` の 5 分類。
  `plan` は作業ドキュメント、`faq` / `knowledge` は随時追記型、`domains` は散文ではなく
  機械可読なドメイン定義 (被覆は Anatomia が別途判定する) のため対象外。
- **「充実」とみなす目安**:
  - 各分類が、そのプロジェクトに該当する範囲を **カバー** している
    （DB があるのに `data/` が無い、外部 API を出すのに `interface/` が無い、等は不充実）。
  - **1 機能 1 ファイル** が守られ、主要機能に対応する `feature/` ファイルがある。
  - ドキュメントが **実装と乖離していない**（古い記述が残っていない）。
- **該当しない分類は減点しない**: 例）DB を持たないツールに `data/` は不要。
  「そのプロジェクトに必要な分類が揃っているか」で見る（一律の枚数は課さない）。
- 評価は [`common/REVIEW_QUALITY.md`](./common/REVIEW_QUALITY.md) §3
  （ドキュメント完備性）のチェック項目で行う。薄い分類はレビューで具体的に指摘する。

---

## モジュール構成のプロジェクト

モジュールを持つプロジェクトでも、まず上記 9 つの標準分類で表現できるか検討する。
固有の仕様は各分類の中で **ファイル名 / サブフォルダ** で区別できる
（例: `feature/<module>-<feature>.md`、`data/<module>.md`）。ただし、責務や正本の性質が
標準分類と異なるなら、モジュール固有の分類フォルダを追加してよい。

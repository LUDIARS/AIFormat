# Spec 生成フォーマット

各プロジェクトはルートに `spec/` フォルダを持ち、プロジェクトの設計仕様を管理する。

---

## 1. ディレクトリ構成

```
spec/
├── schema/           # データスキーマ（DB があるプロジェクト）
├── web/              # フロントエンド構成・API 定義（Web フロントエンドがあるプロジェクト）
├── code/             # クラス・ファイルの役割定義
└── <module>/         # モジュール別仕様（モジュールがあるプロジェクト）
    ├── schema/
    ├── web/
    └── code/
```

各サブディレクトリはプロジェクトの構成に応じて必要なもののみ作成する。

---

## 2. `schema/` — データスキーマ

DB を持つプロジェクトでは `schema/` にデータスキーマを記載する。

### 記載内容

- テーブル定義（カラム名・型・制約・デフォルト値）
- リレーション（外部キー・参照関係）
- インデックス
- ENUM / カスタム型

---

## 3. `web/` — フロントエンド構成・API

Web フロントエンドがあるプロジェクトでは `web/` にフロントエンドの構成と API を記載する。

### 記載内容

- ページ構成（ルーティング・画面一覧）
- API エンドポイント一覧（メソッド・パス・リクエスト / レスポンス）
- 主要コンポーネントの役割
- 状態管理の設計（Context / Store）

---

## 4. `code/` — クラス・ファイルの役割

`code/` にソースコードの構造と各ファイル・クラスの役割を記載する。

### 記載内容

- ディレクトリ構成と各ディレクトリの責務
- 主要クラス・関数の役割と依存関係
- 設計パターン（リポジトリ・アダプタ等）の適用箇所

---

## 5. モジュール分割

モジュールを持つプロジェクトでは、モジュールごとにフォルダを分けてそれぞれの仕様を記載する。

```
spec/
├── schema/              # 共通スキーマ
├── web/                 # 共通フロントエンド
├── code/                # 共通コード構造
├── calendar/            # カレンダーモジュール
│   ├── schema/
│   ├── web/
│   └── code/
├── pm/                  # PM モジュール
│   ├── schema/
│   ├── web/
│   └── code/
└── notification/        # 通知モジュール
    ├── schema/
    ├── web/
    └── code/
```

- 共通部分はルート直下の `schema/` `web/` `code/` に記載
- モジュール固有の仕様はモジュールフォルダ内に記載
- モジュール内も同じ `schema/` `web/` `code/` の構成に従う

# テスト規約 (Test)

LUDIARS 全リポジトリ共通の **テストの規約**。要点は 3 つ:

1. **リリース前にローカル検証を必ず回す** (§1)。
2. **アーキテクチャに従い、どういうテストが必要かをコンテンツごとに設計する** (§2)。
3. **テストの充実度そのものを品質基準とする** (§3)。ローカル検証が green なだけでは
   不十分で、重要経路・異常系・回帰がどれだけ担保されているかをレビューで評価する。

コード側のテスト容易性 (非決定値の注入等) は [`RULE_CODE.md`](./RULE_CODE.md)
§16、品質保証レビューは [`common/REVIEW_QUALITY.md`](./common/REVIEW_QUALITY.md)
を参照。

---

## 1. リリース前にローカル検証を必ず回す — 必須

- GitHub CI / GitHub Actions の workflow は置かない。
- 各リポジトリはリリース前に最低限 **lint / typecheck (or build) / test** を
  ローカルで実行し、失敗した状態を `main` へ公開しない。
- 検証コマンドと結果はローカルの作業記録に残し、GitHub の PR / CI を
  検証記録の正本にしない。
- **バグ修正は再現テストを先に書く** (回帰防止)。修正だけして終わりにしない。

## 2. アーキテクチャに従いテストを設計する — 必須

**「どういうテストが必要か」はコンテンツ (サービス種別 / アーキテクチャ) ごとに
変わる。** 各リポジトリは必要なテストの種類と対象を `spec/` 以下 (例:
`spec/test-plan.md`) に設計として残す。一律のカバレッジ数値は課さず、
**目的・重視点 ([`RULE_CODE.md`](./RULE_CODE.md) 第 I 部) から逆算して** 何を
担保するかを決める。

コンテンツ種別ごとの目安:

### Web サービス
- 単体: リポジトリ層 / 入力バリデーション (Zod スキーマ) / ドメインロジック。
- 統合: REST / WebSocket の `module_request` 経路、認証・認可境界 (未認証で
  破壊的操作が通らないこと)。
- マイグレーションの冪等性 ([`RULE.md`](./RULE.md) §2) を確認する。

### ローカルアプリ (デスクトップ / CLI)
- ローカルデータの整合・破損耐性 (SQLite migration / ファイル I/O)。
- OS / プラットフォーム依存箇所、オフライン動作。
- センシティブ情報の保護経路 ([`RULE_DATA_SCHEMA.md`](./RULE_DATA_SCHEMA.md) §3)。

### ゲーム
- **決定的なロジック** (戦闘 / 状態機械 / ダメージ計算) の単体テスト。非決定値は
  注入可能にして決定的に検証する ([`RULE_CODE.md`](./RULE_CODE.md) §16)。
- リプレイ / シミュレーションによる回帰確認。
- セーブデータの後方互換 (旧フォーマットのロード)。

### 共有ライブラリ / 基盤層
- **公開 API の契約テスト** (シグネチャ・挙動)。
- **consumer のリンク / ビルドテスト** — 特に大規模 squash merge 後は宣言だけ
  取り込んで定義が落ちる事故があるため、利用側 (例: KS) のリンクを必ず確認する。

### レンダリング / パフォーマンス重視
- ホットパスのベンチ / フレーム budget の回帰検知 (タイムライン計測は ergo_profile 等)。

## 3. テストの充実度を品質基準とする — 必須

**「ローカル検証が green」はスタート地点であり、ゴールではない。** テストが
実際に何を守れているか (充実度) を、コード品質と同格の評価軸として扱う。
§2 の方針通り **一律のカバレッジ数値は課さない** が、以下の充実度は満たすことを
必須とする。

### 充実しているとみなす条件

- **重要経路 (critical path) が網羅されている** — そのサービスの存在理由にあたる
  主要ユーザーフロー / コア API が、最低 1 本の通し (integration or smoke) テストで
  毎コミット叩かれている。
- **異常系・境界が書かれている** — 失敗パス (不正入力 / 認可違反 / 外部依存ダウン /
  空・上限・境界値) を「正常系と同じ密度で」テストする。バグの大半は異常系に出る。
- **新機能はテスト同梱で入る** — 振る舞いを足す変更は、その振る舞いを固定する
  テストを同じ変更単位に含める。「後で書く」は負債になるので原則認めない (プロト
  フェーズの粗いコードは、運用に乗せるリファクタの段階でテストを揃える)。
- **回帰テストが資産として残る** — §1 の「バグは再現テスト先行」で書いたテストを
  消さずに残し、同じ回帰を二度踏まない。

### 何を充実とみなすかはアーキから決める

充実度の「対象」は §2 のコンテンツ種別から逆算する。Web なら認可境界と
`module_request` 経路、ゲームなら決定的ロジックとセーブ後方互換、基盤層なら
公開 API の契約と consumer リンク、が「ここが薄いと不充実」と判断する軸になる。
各リポの `spec/test-plan.md` に「何を充実とみなすか」を 1 行でよいので書く。

### レビューでの評価

充実度は [`common/REVIEW_QUALITY.md`](./common/REVIEW_QUALITY.md) §1
(テスト戦略・カバレッジ) のチェック項目で評価する。特に
「コアロジックの unit」「外部 I/O の integration」「主要フローの E2E/smoke」
「異常パスの網羅」が薄い場合は、品質上の指摘としてローカルのレビュー記録に残す。

---

## なぜ？

- リリース前のローカル検証を必須にし、検証済みの変更だけを main へ公開する。
- テストの種類を一律で決めず **アーキ・目的から設計させる** ことで、ゲームに
  Web 用の統合テストを強いる/ライブラリに UI テストを強いる、といった的外れな
  テスト負債を避ける。
- バグの再現テスト先行は、同じ回帰を二度踏まないための最小コストの保険。
- 充実度を品質基準に昇格させるのは、「検証が green」を満たすだけの空テスト
  (正常系を 1 本通すだけ等) で品質を錯覚するのを防ぐため。守れている範囲が
  見えていれば、薄い箇所をレビューで具体的に突ける。
